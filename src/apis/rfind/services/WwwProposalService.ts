
/**
*
* Generated by Rsquare Codegen (v3.0.1) (2022-07-22 14:26:31 192)
*
* 기안서
*
*/


import axios from 'axios'
import { Injectable } from "@nestjs/common"
import { Remote } from '../rfind.config'


/**
* @description 기안서
*/
@Injectable()
export default class WwwProposalService {

  /**
  * @summary  사무실 기안서 
  */
  async getPrdProposal (prdId: string): Promise<string> {
    const data = await axios.get(Remote + '/web/www/proposal/prd-www', {params:{prdId}}) as string
    return new Promise<string>((resolve)=>{resolve(data)})
  }

  /**
  * @summary 
  */
  async getWwwPrdCompareProposal (markers: string[], scale: string, h: string, w: string, center?: string, level?: string): Promise<string[]> {
    const data = await axios.get(Remote + '/web/www/proposal/nmap', {params:{markers, scale, h, w, center, level}}) as string[]
    return new Promise<string[]>((resolve)=>{resolve(data)})
  }

  /**
  * @summary  사무실 비교 기안서 
  */
  async getPrdCompareProposal (prdId?: string[]): Promise<string> {
    const data = await axios.get(Remote + '/web/www/proposal/compare-www', {params:{prdId}}) as string
    return new Promise<string>((resolve)=>{resolve(data)})
  }

}
